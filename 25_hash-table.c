#include <stdio.h>
#include <stdlib.h>
#include <string.h>


int main() {

    // hash table

    // disons une linked list, on doit chercher au travers
    // et qu'elle contient 10,000 éléments,
    // la recherhce pourrait être trop longue

    // idéalement la recherche devrait être O(1)
    // au lieu de O(n)

    // en Python, le dict est en fait un hash table

    

    return 0;
}